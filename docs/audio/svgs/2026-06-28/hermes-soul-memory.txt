欢迎收听每日技术学习语音讲解。本期主题，Hermes SOUL.md 人设工程 + 三层记忆深度解析。标签包括Hermes Agent、SOUL.md、记忆系统、人设工程。本次讲解预计时长 约 12 到 13 分钟，共 11 个部分。好，我们开始。

一句话概括。本文解决的核心问题是：如何用 SOUL.md、MEMORY.md、USER.md 三层 Markdown 文件在约 1,300 Token 的永久记忆预算内定义 Agent 人格、环境事实与用户画像，并理解冻结快照模式、10 层提示词拼装顺序及 9 种外部记忆提供商的选型边界。

接下来，第一部分，核心概念关系。

关系脉络如下：SOUL.md，身份层 #1 槽位，然后AGENTS.md，项目上下文，然后冻结快照，MEMORY + USER，然后Skills 索引，按需加载，然后外部记忆，9 选 1 并行

接下来，第二部分，认知纠偏。

认知纠偏

常见误解：「memory add 写入后 Agent 立刻按新记忆行事」—— Hermes 采用冻结快照模式，会话内系统提示词在启动时拍一次照，中途写入只落盘到文件，要到下次会话才进入提示词；当前轮 Agent 通过工具返回值感知刚写的内容。

接下来，第三部分，反驳。

反驳

对立视角：极简主义 Agent 架构师 / 「上下文即一切」派

你把 1,300 Token 的三文件记忆包装成生产级人设工程，却故意冻结快照、拒绝会话内实时注入——在需要即时纠正 Agent 行为的 Pair Programming 场景里，用户每写一条 memory 都要重开会话，这套「缓存优先」设计把工程便利当成了交互体验的代价。

接下来，第四部分，【概念拆解卡】三层记忆文件分工。

在讲什么问题，三个 Markdown 文件如何分工承载「永远在场」的记忆预算。

核心机制，SOUL.md 占系统提示词 #1 号槽位定义人格语气；MEMORY.md（上限 2,200 字符）存环境事实与项目约定；USER.md（上限 1,375 字符）存用户画像与沟通偏好。三者合计约 1,300 Token，会话启动时冻结注入。

关键理解，人设跟 Hermes 实例走（~/.hermes/），不跟项目目录走；项目规范放 AGENTS.md，临时指令放对话消息。

典型场景，换项目时 SOUL.md 不变、AGENTS.md 随目录切换；跨会话记住「用户偏好 TypeScript」写入 USER.md。

边界说明，内置记忆容量小，只适合核心事实；大规模历史检索靠 session_search 或外部记忆提供商。

「SOUL.md 决定 Agent 用什么腔调说话，MEMORY.md 决定 Agent 知道你的电脑装了什么，USER.md 决定 Agent 知道你喜欢简洁还是详细。」

相关概念：MEMORY 是情景记忆（发生了什么），USER 是语义记忆（你是谁）；与 session_search 的 Tier 2 检索层互补。

接下来，第五部分，【方法/工具卡】SOUL.md 四段式人设编写。

标签：跨项目持久人格 / 系统提示词首位

核心思路：只写真正改变默认行为的内容，按 Identity ，得到 Style ，得到 Avoid ，得到 Defaults 四段组织，30 秒能读完。

操作步骤，

1. 判断指令是否「换项目也生效」——是则放 SOUL.md，否则放 AGENTS.md

2. 用四段式写具体可操作策略（如「遇到坏主意要反驳」「明确说出置信度」）

3. 可选：hermes plugins install LeventeNagy/soul-forge 用 Soul Forge 可视化生成

4. 保存到 ~/.hermes/SOUL.md，经安全扫描后下次会话生效

选型条件，需要临时角色切换用 /personality 预设（14 种内置），退出即恢复 SOUL.md 基线。

落地：写完后开新会话验证语气；用 /personality 对比临时模式与 SOUL 基线差异。

避坑：不要把项目路径、仓库规范、一次性任务写进 SOUL.md；超过 20,000 字符会被截断；含提示词注入或凭据会被 scan 阻止。

接下来，第六部分，【跨概念对比表】SOUL.md vs /personality vs AGENTS.md。

定位，持久基线人格，会话级临时模式，项目级上下文

生命周期，跨所有会话，退出即消失，进入目录时加载

优先级，系统提示词 #1，覆盖 SOUL.md，上下文层（低于身份）

适合内容，语气、风格、默认行为，海盗/侦探等角色扮演，代码规范、架构、路径

存储位置，~/.hermes/SOUL.md，内存 / config 预设，项目目录

接下来，第七部分，【决策/选型表】内置记忆 vs 外部记忆提供商。

核心偏好与机器环境，内置 MEMORY + USER，~1,300 Token 永久在场、零配置，全塞外部提供商，丢失简洁性与启动速度

跨会话用户建模，Honcho（方言式推理），双层上下文 + 用户/AI 表示卡，仅靠 MEMORY 堆条目，2,200 字符很快满

查找历史对话细节，session_search（FTS5），无限容量、按需 ~20ms 查询，把日志写入 MEMORY，挤占永久预算、违反条目规范

大规模结构化事实，Mem0 / Zep 等外部商，与内置并行、单提供商激活，同时开多个外部商，Hermes 约束同时只能一个

接下来，第八部分，【避坑清单卡】记忆写入与容量管理。

坑名，会话中途写入记忆却期望系统提示词立刻变化

原因，冻结快照为提示词缓存优化，会话内系统提示词不变

原文说法：「当 Agent 通过 memory add 写入新条目时，更改持久化到磁盘，但不影响当前已构建的系统提示词。」

解法，关键事实写入后开新会话；或依赖工具返回值让 Agent 当轮感知

严重程度，小心（行为符合设计但易误解）

坑名：MEMORY 超 80% 仍盲目 add ，得到 返回满容量错误，解法：先 merge 冗余条目或 remove 过时项；条目要紧凑信息密集，禁存代码块与易重发现事实，严重程度：致命（工具直接失败）

接下来，第九部分，【心法/原则卡】冻结快照是为成本与一致性。

原则，记忆「拍快照」不是偷懒，是为提示词缓存命中、Token 成本降低约 75% 与会话内行为一致。

为什么重要，实时更新会让每轮系统提示词变化、缓存失效、全价重处理。

原文支撑：10 层拼装将 SOUL/工具指引放稳定层，MEMORY/USER 快照放易变层但会话内冻结。

怎么落地，调试人设改 SOUL.md 后开新会话；调 MEMORY 看容量头 80% 预警；项目规范写 AGENTS.md 而非重复存入 MEMORY。

适用边界，需要「写入立刻改变全局行为」的场景应改 SOUL/AGENTS 或结束当前会话，而非指望中途快照刷新。

接下来，第十部分，总结与行动。

结论

SOUL.md 占 #1 槽位定义跨项目人格；MEMORY/USER 合计约 1,300 Token 承载事实与画像

冻结快照：会话启动拍一次照，中途 memory 写入落盘但不改已构建提示词

10 层拼装分稳定层、上下文层、易变层，利于缓存与调试

外部记忆 9 选 1 与内置并行；历史细节用 session_search FTS5

SOUL 写语气边界，AGENTS 写项目规范，/personality 做临时角色

按四段式起草 ~/.hermes/SOUL.md，剔除项目路径与一次性指令

用 memory add 写入 3-5 条紧凑环境事实，观察容量百分比

在项目根建 AGENTS.md 放代码规范，验证切换目录时 SOUL 不变

容量超 80% 时 merge/remove 后再 add

需要大规模记忆时评估 Honcho 或 Mem0，确认只激活一个外部商

总结：

行动清单：

关键认知转变：记忆不是「实时数据库」而是「会话启动时的提示词配料」——理解冻结快照才能正确预期 memory 工具的生效时机。

讲解完毕。建议对照页面上的 SVG 卡片复习要点，并阅读原文加深理解。祝学习顺利！
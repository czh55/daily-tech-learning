欢迎收听每日技术学习语音讲解。本期主题，2 名工程师 + Codex，OpenAI 用 Rust 重写扛住 10 亿人的存储系统。标签包括Habitat、Python → Rust、高并发调优、AI 辅助迁移。本次讲解预计时长 约 16 到 17 分钟，共 11 个部分。好，我们开始。

一句话概括。本文解决的核心问题是：在两年 1000 倍请求增长、Python 高并发踩遍 asyncio 调度、连接池 LIFO 亚稳态失败等坑之后，OpenAI 如何用「战略性技术负债 + 受限 API + 2 人 Codex 重写」把 Habitat 从客户端库演进为扛住 7000 万 QPS 的 Rust 服务。

接下来，第一部分，核心概念关系。

关系脉络如下：Python 客户端库，2024 年中，然后独立服务，运维事故倒逼，然后Python 四场硬仗，调优撑到 2000 万 QPS，然后Rust 重写，2 人 + Codex，然后7000 万 QPS，CPU 6× / 内存 15×

接下来，第二部分，认知纠偏。

认知纠偏

常见误解：「OpenAI 用 Rust 重写 = Python 不适合高并发，一开始就该选 Rust」。团队明确承认 Python 在 100 倍规模下不可接受，但刻意先用 Python 换速度、解除产品阻塞，并押注内部代码模型会让未来迁移可行——这是一次计算过的战略性技术负债，不是技术选型失误。

接下来，第三部分，反驳。

反驳

对立视角：「Day 1 Rust」派 · 基础设施洁癖主义者

战略性负债听起来理性，实则把两年调优血泪和亚稳态事故成本转嫁给运维；若 2024 年就选 Rust，7000 万 QPS 不必先拿 Python 当人肉压测。

接下来，第四部分，【概念拆解卡】Habitat 与「战略性技术负债」。

在讲什么问题，Habitat 是 OpenAI 统一在线存储平台，支撑 ChatGPT/Codex/API 全部产品的数据读写，两年内从单库 Python 客户端成长为 7000 万 QPS、10 亿周活、500PB 数据的分布式系统。

核心机制，产品工程师通过受限 NoSQL API（对象+边，参考 Meta TAO）存取数据，Habitat 包办路由、鉴权、加密、序列化、连接池等脏活；故意不做任意 SQL，用可预测开销换可扩展性。

关键理解，团队明知 Python 低效，仍选择先立服务——最紧迫目标是解除产品阻塞、打稳平台基础，同时赌 Codex/GPT 未来能简化迁移；2026 Q2 2 名工程师一季完成 Rust 重写，赌注兑现。

典型场景，用户发一条 ChatGPT 消息可能触发数百次 Habitat 读写；任一次慢或失败直接体现在产品卡顿/崩溃上。

边界说明，受限 API 不适合需要复杂图遍历/联表的分析场景——这类需求走 CDC 同步到 Rockset 的「逃生通道」，在线存储与分析负载隔离。

原文：我们也下了一个「计算好的赌注」：赌自己内部代码模型的飞速进步，会在未来大大简化这次技术栈迁移的路径。

接下来，第五部分，【跨概念对比表】客户端库 vs 独立服务 vs Rust 重写。

发布协调，改一行要协调几十个团队，改一个服务，全产品受益，同左，效率再升 6×，库模式在规模下必然崩溃

并发模型，本地直连，延迟低，asyncio + GIL，调度抖动数百毫秒，无 GIL，原生高效，服务化牺牲延迟换统一治理

连接管理，各客户端各自连接池，LIFO 引发亚稳态失败，原生更高效 + Envoy 聚合，连接策略是隐藏杀手

峰值吞吐，—，撑住 2000 万 QPS，7000 万 QPS，95% 流量，Python 是过渡而非终点

迁移成本，—，—，2 人 + Codex 一季完成，AI 已能承接核心基建迁移

接下来，第六部分，【避坑清单卡】Python 高并发四场硬仗。

坑 1 — asyncio 调度延迟：下游 Cosmos DB 已返回，请求却卡在等协程被重新调度；高 CPU 下事件循环抖动可达数百毫秒甚至数秒。并发不等于并行，GIL 让 CPU 密集任务与 I/O 争抢调度。

解法，专门监控 asyncio 事件循环繁忙程度，压低单进程并发、横向扩大量 worker 进程。

坑 2 — Statsig 整点卡顿：每分钟全 Pod 8 进程同时拉取巨大 feature flag 配置，集体卡顿解析，正在处理的请求被晾在一边。

解法，下发更小更精准的目标化配置、拉长刷新间隔、后台任务加随机 jitter。

坑 3 — 连接池 LIFO 亚稳态失败：aiohttp TCPConnector 默认 LIFO 复用最近连接，高并发客户端只打少数服务进程，尾部进程负载 5～10 倍均值；停掉突增客户端后部分进程仍亚健康直至手动重启。

解法，限制最大连接复用时长验证猜想，再将 LIFO 改为 FIFO 彻底打破恶性反馈循环。

坑 4 — 下游连接风暴：大量 Python 进程各自建连，日常发布批量重建连即可 CPU 抖动，连接泄漏可打爆 NAT 网关。

解法，Envoy 统一 HTTP/2 多路复用、连接池化与生命周期管理，集中限流熔断。

严重程度：坑 3 最隐蔽（亚稳态需手动重启）；坑 1/4 在 Habitat 进程数量高出一个数量级时触发门槛极低。

接下来，第七部分，【决策/选型表】存储架构与语言迁移时机。

产品阻塞、平台未稳，先用 Python 立服务，接受技术负债，速度优先，同时押注 AI 辅助未来迁移，一开始就追求完美 Rust 实现，两个问题（架构+语言）同时上会拖死交付

协议升级需协调多团队，从库拆成独立服务，统一发布、可观测性、安全合规集中控制，继续客户端库 + feature flag 协调，几十个团队脆弱协调，出错概率线性上升

在线读写 API 设计，受限 NoSQL（对象+边，无图遍历），开销可预测，水平扩展简单，开放任意 SQL 查询，一条昂贵查询即可拖垮全库

复杂分析/搜索需求，CDC 同步到 Rockset，团队自扩容，在线存储与分析负载隔离，在 Habitat 层支持复杂联表，扇出不可控，制造延迟悬崖

Python 已撑到 2000 万 QPS、平台成熟，启动 Rust 重写，借助 Codex/GPT，CPU 6×、内存 15×，消灭 GIL/asyncio 根源，无限 Python 调优而不还债，100 倍规模下 Python 低效不可接受

客户端连接池策略，FIFO 或 Envoy/Istio 集中管理，避免 LIFO 制造负载倾斜与亚稳态，默认 LIFO 不做评估，大多数场景合理，高并发服务是例外

接下来，第八部分，【方法/工具卡】连接池与 Envoy 调优实操路径。

方法名：连接池策略审计 + Envoy 连接聚合 · 标签：高并发、负载均衡

核心思路：客户端连接池的复用策略（LIFO/FIFO）会直接影响服务端进程间负载分布，错误策略可制造亚稳态失败；大量进程各自建连则引发惊群效应。

操作步骤，1) 检查 HTTP 客户端连接池默认策略（aiohttp 默认 LIFO），得到 2) 观察服务进程利用率方差，尾部是否 5～10 倍均值 ，得到 3) 限制最大连接复用时长做 A/B 验证 ，得到 4) 将 LIFO 改为 FIFO 或迁移到 Envoy HTTP/2 多路复用 ，得到 5) 在 Envoy 层集中实施限流、熔断与连接生命周期管理 ，得到 6) 为 feature flag/配置拉取加 jitter，避免全进程同步卡顿

落地建议：除 CPU/内存/网络/磁盘外，必须监控 asyncio 事件循环繁忙度；横向扩进程时同步评估下游 NAT/连接数上限，按峰值连接而非稳态吞吐量规划网络资源。

避坑：不要以为「并发高 = 并行好」——asyncio 在 CPU 密集路由/加密/校验和场景下调度抖动是尾延迟元凶；也不要假设连接池默认值在所有规模下都安全。

接下来，第九部分，【心法/原则卡】用约束换可扩展性的 Habitat 设计哲学。

原则，「简单、可预测、开销恒定」的请求系统，远比功能强大但开销不可控的系统更容易扩展、更不容易被误用。

为什么重要，写一条昂贵 SQL 太容易，人肉 review 在团队扩张后迅速失控；Habitat 让「贵」在客户端层面变得显眼，倒逼更高效设计。

怎么落地，对象与边放在同一存储分区以利水平扩展；图遍历效率低是刻意代价；复杂查询走 Rockset 逃生通道，把复杂度显式推给需要的客户端。

适用边界，适合在线读写、扇出可控的场景；不适合需要实时复杂图分析且不愿承担 CDC+Rockset 接入成本的团队。

原文：那些可能带来不可预期扇出的请求，才是真正危险的——它们会让隔离、负载均衡都变得复杂，还会制造难以扩容应对的延迟悬崖。

接下来，第十部分，总结与行动。

结论

Habitat 两年从 Python 库成长为 7000 万 QPS 分布式存储，核心转折是从客户端库拆成独立服务，解决多团队协调的运维地狱。

团队明知 Python 在百倍规模不可接受，仍战略性先用 Python 换速度，并押注 Codex/GPT 简化迁移——2026 Q2 2 人一季 Rust 重写兑现赌注。

Python 阶段四场硬仗（asyncio 调度、Statsig 整点卡顿、LIFO 亚稳态失败、连接风暴）是真实生产血泪，Rust 从语言层消灭至少三个根源。

受限 NoSQL API + CDC/Rockset 逃生通道，用可预测性换可扩展性，把复杂查询复杂度显式推给客户端。

Rust 版 CPU 效率 6 倍、内存 15 倍，已承接 95% 生产流量，Python 版数周内退役。

审计现有 HTTP 客户端连接池策略，高并发服务评估 LIFO 是否制造负载倾斜，考虑 FIFO 或 Envoy 集中管理。

为 feature flag/配置拉取加 jitter，避免全进程同步解析造成周期性尾延迟尖刺。

监控 asyncio 事件循环繁忙度（Python 服务），并压低单进程并发、横向扩展 worker。

设计存储 API 时优先「可预测开销」而非「功能强大」，为复杂查询预留隔离出口（如 CDC+分析引擎）。

若计划语言级迁移，评估当前 AI 代码模型能力，将架构问题（库 vs 服务）与语言问题分阶段解决。

总结：

行动清单：

关键认知转变：极端增长下的工程智慧不是「一开始就选对语言」，而是清楚自己在赌什么、何时还债——先用 Python 解阻塞、用约束换扩展性，再在 AI 辅助下高效完成核心基建的语言级迁移，这比追求 Day 1 完美技术栈更接近真实大厂演进路径。

讲解完毕。建议对照页面上的 SVG 卡片复习要点，并阅读原文加深理解。祝学习顺利！
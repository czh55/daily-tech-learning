欢迎收听每日技术学习语音讲解。本期主题，DeepMind 亮出王炸：别再手写 Agent Harness 了，AI 已经学会自己写了！。标签包括Agent Harness、AutoHarness、DeepMind、人机协同。本次讲解预计时长 约 12 到 13 分钟，共 11 个部分。好，我们开始。

一句话概括。本文解决的核心问题是：当大模型在规则博弈中近八成败于「犯规」而非技不如人时，DeepMind 的 AutoHarness 如何用「AI 自写代码护栏」替代手工 Harness，以及人类工程师应转向环境设计与评估体系构建。

接下来，第一部分，核心概念关系。

关系脉络如下：裸奔 LLM，语言强 / 规则弱，然后Code Harness，规则校验器，然后AutoHarness，试错，得到批评，得到精炼，然后环境设计 + Evals，人类新角色

接下来，第二部分，认知纠偏。

认知纠偏

常见误解：「AutoHarness 意味着手写 Harness 立刻过时」—— 原文明确，极高安全要求与复杂业务领域仍需人类专家经验；变化的是重心从「亲手写规则」转向「设计能自学习规则的环境与评估标准」。

接下来，第三部分，反驳。

反驳

对立视角：企业安全合规团队 / 「护栏必须人审」派

145 个文本游戏里平均 14.5 次迭代就能收敛，换成支付清结算或多级审批的业务系统，没有人类写死的中间件、审计链和变更评审，AutoHarness 自生成的护栏根本过不了合规验收，更扛不住一次错误放行的资金损失。

接下来，第四部分，【概念拆解卡】Agent Harness 与 AutoHarness。

在讲什么问题，大模型为何需要外部「驾驭系统」，以及 DeepMind 如何让 AI 自己生成这套系统。

核心机制，Harness 是给「毛坯大脑」装配的规则护栏；AutoHarness 让模型根据环境反馈，迭代生成含 propose_action() 与 is_legal_action() 的 Python 校验代码。

关键理解，模型擅长模仿「像正确答案的文本」，却不天然遵守当前状态下的硬规则——护栏把概率生成约束在合法动作空间内。

典型场景，棋类对战、文本游戏、任何有明确合法性判定的 Agent 决策环境。

边界说明，依赖环境能提供清晰、即时的合法/非法反馈；模糊语义、多步审批、合规审计链难以仅靠自动迭代收敛。

原文：「大模型本身只是一个毛坯大脑，你必须为它手工打造一套精密的外部骨骼（Harness），它才能真正干活。」

与 ReAct 循环的关系：ReAct 解决「怎么想、怎么调工具」；Harness 解决「想出来的动作是否合法」——二者叠加才构成完整 Agent 栈。

接下来，第五部分，【方法/工具卡】AutoHarness 四步自我进化闭环。

方法名：Code-as-Harness 迭代精炼

核心思路：用并行环境试错 + LLM 批评家/精炼器，把错误反馈转化为代码补丁，树搜索式逼近 100% 合法动作校验。

操作步骤，1. 基础模型生成初始策略代码（含合法走步函数），得到 2. 10 路并行环境运行，非法步或执行错误即终止并采集反馈 ，得到 3. 批评家整理错误，精炼器 LLM 改写代码 ，得到 4. 重新投入环境，循环直至收敛。

选型条件，规则可程序化判定、反馈信号明确、可承受十余次迭代的算力成本时优先于手工 Harness。

原文：「在 145 个不同的文本游戏中，AutoHarness 平均只需要 14.5 次迭代，就能生成达到 100% 准确率的合法走步校验器。」

落地建议：为业务 Agent 先抽象「最小可试错沙箱」：定义动作 API、合法判定函数、结构化错误码，再让模型在沙箱内自写 is_legal_action 类校验逻辑。

避坑：环境反馈含糊（如「操作失败」无原因）会导致精炼器盲改；必须像 DeepMind 游戏环境一样给出可定位的错误信息。

接下来，第六部分，【跨概念对比表】微调 vs 手写 Harness vs AutoHarness。

规则遵守，概率性内化，难保证 100%，人类硬编码，可精确，迭代至环境反馈收敛，要确定性护栏别只靠微调

成本与速度，数据+算力极高，迭代慢，每换场景重写，脆弱，平均 14.5 次迭代/游戏，AutoHarness 规模化更优

泛化能力，可能损害其他任务，场景绑定强，依赖环境抽象质量，通用能力 vs 规则精度需权衡

人机分工，数据标注者，规则手工艺人，环境设计师+评估者，人的价值上移到 Evals

接下来，第七部分，【避坑清单卡】裸奔 Agent 的三类典型失败。

坑名，非法动作泛滥——Kaggle 国际象棋赛中 Gemini 2.5 Flash 78% 败局源于 Illegal Moves。

原因，模型按训练数据概率模仿「像合法走法」的文本，未绑定当前棋盘状态约束。

解法，部署前必须为 Agent 装配 Harness 或 AutoHarness 生成的 is_legal_action 校验层，禁止裸模型直接输出动作。

严重程度，致命——无护栏时大模型 Pro 对局胜率仅 38.2%，装备护栏的小模型 Flash 胜率达 56.3%。

原文：「它会尝试让马走直线，或者把兵横着走。」

延伸坑：以为「模型越大越不需要护栏」——实验表明 Flash+Harness 可碾压裸奔的 Pro，Harness 价值与模型规模正交。

接下来，第八部分，【决策/选型表】Harness 建设路径怎么选。

规则清晰的游戏/仿真环境，AutoHarness 自动生成，145 游戏验证，迭代成本低，纯微调学规则，代价高且难 100% 合法

金融/医疗/权限审批，人类手写 Harness + 人工审批，安全边界与审计链不可自动化试错，完全 AutoHarness，合规验收无法仅靠环境反馈

成本敏感在线推理，Harness-as-Policy 纯代码策略，运行时零 LLM 调用，小模型代码可超 GPT-5.2-High 均分，每步调大模型，延迟与 Token 成本爆炸

快速原型验证，手写最小 Harness + Evals，先跑通再考虑自动化合成，直接裸 Agent 上线，非法动作与死循环在生产才暴露

接下来，第九部分，【心法/原则卡】从「教它做事」到「教它学习」。

原则，把 AI 当能自我反思的初级程序员——提供测试用例与明确错误反馈，放手让它进化规则，而非手把手写每一行护栏。

为什么重要，人类终极护城河是定义「什么是好的 Harness」——环境设计与 Evals 架构，而非重复劳动写校验器。

适用边界，递归自我改进仍处早期；复杂领域人类专家经验与 Safety Middleware 不可替代。

原文：「当 AI 能写 Harness 时，我们人类的终极护城河，就变成了定义什么是好的 Harness 的能力。」

怎么落地：① 将业务抽象为可安全试错的模拟环境 ② 建设批评家/裁判式多 Agent 评估链 ③ 为 Harness 定义 Token 成本、安全边界、跑分基准。

接下来，第十部分，总结与行动。

结论

大模型语言能力强但规则遵守弱，无 Harness 时近八成棋局败于犯规而非策略差。

AutoHarness 用试错，得到批评，得到精炼闭环，平均十余次迭代即可合成 100% 合法动作校验器。

小模型+护栏可击败裸奔大模型，Harness-as-Policy 甚至能在单人游戏中超越顶级模型均分。

工程师角色从规则手工艺人转向环境设计师与评估体系架构师。

手写 Harness 不会立刻消失，但重心从授人以鱼变为授人以渔。

盘点现有 Agent 是否存在「裸奔」路径，为关键动作增加 is_legal 类校验层。

为下一 Agent 项目设计最小可试错沙箱：结构化动作 API + 明确错误反馈。

建立 Harness Evals：合法率、胜率、Token 成本、安全拦截率四类基准指标。

阅读 AutoHarness 论文（arXiv:2603.03329），对照自家业务判断哪些场景可尝试自动合成护栏。

高安全领域保留人工 Harness 与审批链，仅用 AutoHarness 思路优化内部仿真环境。

总结：

行动清单：

关键认知转变：Harness Engineering 的价值不在「亲手写每一行规则」，而在「设计让 AI 能自己学规则的环境，以及判定好坏的评估体系」——这才是 AI 自我进化前夜人类架构师真正的壁垒。

讲解完毕。建议对照页面上的 SVG 卡片复习要点，并阅读原文加深理解。祝学习顺利！
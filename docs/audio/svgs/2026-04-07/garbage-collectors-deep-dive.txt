欢迎收听每日技术学习语音讲解。本期主题，从 1960 到 2026：一文看透 Java、Go、Python 垃圾回收器的原理与演进。标签包括Go/AI 技术、工程实践、深度解读。本次讲解预计时长 约 12 到 13 分钟，共 10 个部分。好，我们开始。

一句话概括。本文解决的核心问题是：为什么 Java 的 G1GC 需要设置停顿目标？Go 的混合写屏障是如何消除栈重扫的？Python 又是如何解决引用计数无法处理的循环引用？

接下来，第一部分，核心概念关系。

关系脉络如下：McCarthy (1960): Rec，然后Wilson (1992): Unipr，然后两种基本方法，然后Reference Counting (

接下来，第二部分，认知纠偏。

认知纠偏

常见误解：只记住结论不验证边界——读完应能回答「什么场景适用、什么场景不该用」，而非背诵作者态度。

接下来，第三部分，反驳。

反驳

对立视角：「架构即文档」派

所谓过度架构往往是事后判断——在需求剧烈变化期，多一层抽象可能是唯一让团队不崩溃的缓冲，极简主义不是万能药。

接下来，第四部分，【概念拆解卡】McCarthy (1960): Recursive Functions of Symbolic Expressions and Their Computation by Machine。

在讲什么问题，McCarthy (1960): Recursive Functions of Symbolic Expressions and Their Computation by Machine

核心机制，这篇论文因引入 Lisp 而闻名，但垃圾回收器几乎是作为实现细节被埋藏在其中的。

关键理解，这篇论文因引入 Lisp 而闻名，但垃圾回收器几乎是作为实现细节被埋藏在其中的。McCarthy 需要一种方法来管理符号表达式的内存。Lisp 程序操作的是嵌套的列表（lists of lists of lists），这种递归结构使得要求程序员手动释放内存变得不切实际。因此，他描述了一种自动执行此操作的机制。 该机制分为两个阶段。首先，从程序正在活跃使用的 root（根）变量开始，遍历它们引用的每一个对象，将每个对象标记为 reachable（可达）。其次，扫描所有内存。任何未被标记的对象都是垃圾。将它们重新添加回 free list（空闲列表）。 这就是 mark-and-sweep（标记-清除）。它能自然地处理 cycles（循环引用，因为不可达的循环永远不会被标记），不需要逐个对象的簿记工作，并让程序员可以完全忽略内存管理。 其代价是程序在收集器运行时必须完全停止。每一次分配、每一次计算，所有一切都会冻结，直到标记和清除完成。对于 McCarthy 在 1960 年编写的程序来说，这完全是合理的。 随着程序规模变大并进入对延迟敏感的环境（如处理每秒数千次请求的 Web 服务器），stop-the-world（全线停顿）成了一个难以接受的权衡。现代 GC 研究产生的大部分成果都是为了回答一个问题：如何在不停止世界的情况下进行垃圾内存回收？

典型场景，当团队讨论与本文主题相关的技术选型或工程决策时。

边界说明，需结合自己的规模、栈和合规要求裁剪，不可无脑复制。

原文依据：这篇论文因引入 Lisp 而闻名，但垃圾回收器几乎是作为实现细节被埋藏在其中的。McCarthy 需要一种方法来管理符号表达式的内存。

接下来，第五部分，【方法/工具卡】Wilson (1992): Uniprocessor Garbage Collection Techniques。

核心思路：到 1992 年，三十年的 GC 研究已经产生了许多想法，但缺乏统一的词汇。

操作步骤，1. 阅读原文场景 2. 提取可执行动作 3. 小范围试点 4. 对照原文指标验收

选型条件，当团队面临与原文类似约束时优先采用。

原文引用：到 1992 年，三十年的 GC 研究已经产生了许多想法，但缺乏统一的词汇。Wilson 的综述论文将这一切组织了起来。

落地建议：本周选 1 个与「Wilson (1992): Uniprocessor Garbage Collection Techniques」相关的任务做对照验证。

避坑：不要跳过边界说明直接照搬工具链或配置。

接下来，第六部分，【避坑清单卡】两种基本方法。

坑名，几乎所有的垃圾回收器要么是 reference counting（引用计数），要么是 tracing（追踪），或者是两者的某种结合。

原因，几乎所有的垃圾回收器要么是 reference counting（引用计数），要么是 tracing（追踪），或者是两者的某种结合。Wilson 的论文围绕这一划分进行组织，三十年后依然成立。

解法，对照原文边界说明，在团队中建立「什么场景不该用」的检查清单。

严重程度，小心——不验证边界时容易照搬结论。

原文：几乎所有的垃圾回收器要么是 reference counting（引用计数），要么是 tracing（追踪），或者是两者的某种结合。Wilson 的论文围绕这一划分进行组织，三十年后依然成立。

接下来，第七部分，【决策/选型表】Reference Counting (引用计数)。

原文主场景，按作者方案执行，每个对象维护一个指向它的引用计数。，跳过验证直接照搬，忽略边界导致踩坑

资源/预算受限，裁剪到最小可行子集，先验证核心价值假设，全量一次性上线，失败成本高、回滚难

团队经验不足，小范围试点 + 对照实验，降低 Blast Radius，直接替换现有流程，组织惯性被低估

接下来，第八部分，【心法/原则卡】Tracing (追踪式，即 Mark-and-Sweep)。

原则，追踪式收集器不跟踪单个引用，而是从一组已知的存活引用（称为 root set，根集合）开始，遍历整个对象图。

为什么重要，追踪式收集器不跟踪单个引用，而是从一组已知的存活引用（称为 root set，根集合）开始，遍历整个对象图。它能触达的每个对象都被标记为存活。其他所有对象都被释放。 Root set 是起点，因此什么算作 root（根）至关重要。不同语言的答案是相同的：root 是 runtime（运行时）无需追踪就能找到的任何引用。这些指针锚定在程序当前的执行状态中，是在任何遍历开始之前你就知道是存活的东西。 在实践中，roots 分为以下几类。 每个活跃 stack frame（栈帧）中的 local va

怎么落地，把原则写成团队 checklist，在 code review 或设计评审中强制过一遍。

适用边界，资源极度受限或一次性原型场景可适度放宽。

原文支撑：追踪式收集器不跟踪单个引用，而是从一组已知的存活引用（称为 root set，根集合）开始，遍历整个对象图。它能触达的每个对象都被标记为存活。

接下来，第九部分，总结与行动。

结论

这篇论文因引入 Lisp 而闻名，但垃圾回收器几乎是作为实现细节被埋藏在其中的。

到 1992 年，三十年的 GC 研究已经产生了许多想法，但缺乏统一的词汇。

几乎所有的垃圾回收器要么是 reference counting（引用计数），要么是 tracing（追踪），或者是两者的某种结合。

每个对象维护一个指向它的引用计数。

追踪式收集器不跟踪单个引用，而是从一组已知的存活引用（称为 root set，根集合）开始，遍历整个对象图。

结合「McCarthy (1960): Recursive Functions of Symbolic Expressions and Their Computation by Machine」在本周做 1 次小范围对照验证

结合「Wilson (1992): Uniprocessor Garbage Collection Techniques」在本周做 1 次小范围对照验证

结合「两种基本方法」在本周做 1 次小范围对照验证

把本文一个关键认知转变写成 3 句话分享给同事

总结：

行动清单：

关键认知转变：从「记住结论」到「能判断何时该用、何时不该用」——边界感比信息量更重要。

讲解完毕。建议对照页面上的 SVG 卡片复习要点，并阅读原文加深理解。祝学习顺利！
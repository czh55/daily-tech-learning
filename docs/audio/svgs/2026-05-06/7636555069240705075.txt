欢迎收听每日技术学习语音讲解。本期主题，LangChain DeepAgents 速通指南（八）—— DeepAgents流式输出详解。标签包括AI 架构、前端工程、Prompt 工程。本次讲解预计时长 约 13 到 14 分钟，共 10 个部分。好，我们开始。

一句话概括。本文解决的核心问题是：需要构建打字机效果的聊天界面，并监控工具调用？选messages。？

接下来，第一部分，核心概念关系。

关系脉络如下：一、流式输出为什么重要?，然后二、架构基石：理解DeepAgents的，然后三、DeepAgents流式输出实战，然后四、Stream Mode 模式详解与对

接下来，第二部分，认知纠偏。

认知纠偏

常见误解：「GitHub Stars 最高 = 最好」—— 应综合看 CVE 记录、场景匹配和真实成本，而非关注度排名。

接下来，第三部分，反驳。

反驳

对立视角：Geohot 式 Agent 怀疑派

自主 Agent 的 Token 成本与错误决策半径常被低估——Stars 和 demo 视频好看，生产环境里的死循环、权限事故和 $131/天账单才是真实账单。

接下来，第四部分，【概念拆解卡】一、流式输出为什么重要?。

在讲什么问题，一、流式输出为什么重要?

核心机制，试想一下：当大家在终端里输入“分析远程办公对团队生产力的影响”，随后盯着漆黑的屏幕足足几分钟，既没有任何反馈，也没有任何进度提示。

关键理解，试想一下：当大家在终端里输入“分析远程办公对团队生产力的影响”，随后盯着漆黑的屏幕足足几分钟，既没有任何反馈，也没有任何进度提示。这种“体验灾难”足以劝退大半的大模型应用用户——流式输出的出现，正是为了解决这个问题。 流式输出让智能体在执行过程中，每一刻都能向用户汇报进度：模型正在推理、子 Agent 已启动、工具正在调用、Token 逐字生成……这些实时信号不仅能安抚用户的等待焦虑，更是构建大模型应用的基础能力。 DeepAgents 在 LangGraph 强大的流式输出基础上，进一步提供了对子 Agent 的流式支持（毕竟多智能体协作也是DeepAgents的核心特性之一）。当主 Agent 通过task工具将任务委派给子 Agent 时，开发者可以独立地从每一个子 Agent 中流式获取大模型输出和工具调用。这种多层次、可溯源的流式能力，正是 DeepAgents 区别于普通单 Agent 框架的核心特点。

典型场景，当团队讨论与本文主题相关的技术选型或工程决策时。

边界说明，需结合自己的规模、栈和合规要求裁剪，不可无脑复制。

原文依据：试想一下：当大家在终端里输入“分析远程办公对团队生产力的影响”，随后盯着漆黑的屏幕足足几分钟，既没有任何反馈，也没有任何进度提示。这种“体验灾难”足以劝退大半的大模型应用用户——流式输出的出现，正是为了解决这个问题。

接下来，第五部分，【方法/工具卡】二、架构基石：理解DeepAgents的流式机制。

核心思路：在动手编写代码之前，笔者先简单讲解一下 DeepAgents 的底层流式架构，这有助于大家更快理解接下来的流式模式和事件路由。

操作步骤，1. 阅读原文场景 2. 提取可执行动作 3. 小范围试点 4. 对照原文指标验收

选型条件，当团队面临与原文类似约束时优先采用。

原文引用：在动手编写代码之前，笔者先简单讲解一下 DeepAgents 的底层流式架构，这有助于大家更快理解接下来的流式模式和事件路由。DeepAgents 底层采用协调器-工作者架构（对该结构感兴趣的读者可阅读笔者的LangGraph设计模式文章《 LangGraph智能体开发设计模式（二）——协调器-工作者模式、评估器-优化器模式》））：主 Agent 负责任务规划与委派，每个子 Agent 在自己隔离的沙箱中独立执行，彼此互不干扰。

落地建议：本周选 1 个与「二、架构基石：理解DeepAgents的流式机制」相关的任务做对照验证。

避坑：不要跳过边界说明直接照搬工具链或配置。

接下来，第六部分，【避坑清单卡】三、DeepAgents流式输出实战。

坑名，了解了基本概念后，必须通过实战来检验一下。

原因，了解了基本概念后，必须通过实战来检验一下。本示例将沿用《LangChain DeepAgents 速通指南（六）—— DeepAgents SubAgent 子智能体机制》中的代码，完整的代码笔者会在文章最后列出。该智能体的功能为：通过研究子 Agent 查阅资料，再通过总结子 Agent 撰写报告。 research_subagent = { &quot;name&quot;: &quot;research-agent&quot;, &quot;description&quot;: &quot;用于深度搜索网络信息&quot;, &quot;system_prompt&quot;: &quot;你是一个网络搜索大师，可以调用网络搜索工具搜索用户想了解的内容&quot;, &quot;tools&quot;: [internet_

解法，对照原文边界说明，在团队中建立「什么场景不该用」的检查清单。

严重程度，小心——不验证边界时容易照搬结论。

原文：了解了基本概念后，必须通过实战来检验一下。本示例将沿用《LangChain DeepAgents 速通指南（六）—— DeepAgents SubAgent 子智能体机制》中的代码，完整的代码笔者会在文章最后列出。

接下来，第七部分，【决策/选型表】四、Stream Mode 模式详解与对比。

原文主场景，按作者方案执行，掌握了基础的事件流式与命名空间路由之后，大家可能也发现：stream_mode='updates'模式每次依然会等到信息积攒到一定量才输出，和常见的那种“逐字流式”体验还有差距。，跳过验证直接照搬，忽略边界导致踩坑

资源/预算受限，裁剪到最小可行子集，先验证核心价值假设，全量一次性上线，失败成本高、回滚难

团队经验不足，小范围试点 + 对照实验，降低 Blast Radius，直接替换现有流程，组织惯性被低估

接下来，第八部分，【心法/原则卡】五、总结。

原则，本期笔者系统性地分享了 DeepAgents 流式输出的核心路径，并对stream_mode的几种模式进行了横向拆解与对比。

为什么重要，本期笔者系统性地分享了 DeepAgents 流式输出的核心路径，并对stream_mode的几种模式进行了横向拆解与对比。从架构层面的命名空间路由，到updates、messages、custom三大粒度模式的实战，再到生产级的多模式组合——每个环节都配有可运行的代码示例，希望大家能真正掌握这套流式体系。 最后，再划一遍重点，送上几条最佳实践： 1. 始终使用version=&quot;v2&quot; ：统一的StreamPart字典格式消除了不同流式模式下的结构差异，让事件处理代码更加一致。 2. 用命名空间

怎么落地，把原则写成团队 checklist，在 code review 或设计评审中强制过一遍。

适用边界，资源极度受限或一次性原型场景可适度放宽。

原文支撑：本期笔者系统性地分享了 DeepAgents 流式输出的核心路径，并对stream_mode的几种模式进行了横向拆解与对比。从架构层面的命名空间路由，到updates、messages、custom三大粒度模式的实战，再到生产级的多模式组合——每个环节都配有可运行的代码示例，希望大家能真正掌握这套流式体系。

接下来，第九部分，总结与行动。

结论

试想一下：当大家在终端里输入“分析远程办公对团队生产力的影响”，随后盯着漆黑的屏幕足足几分钟，既没有任何反馈，也没有任何进度提示。

在动手编写代码之前，笔者先简单讲解一下 DeepAgents 的底层流式架构，这有助于大家更快理解接下来的流式模式和事件路由。

了解了基本概念后，必须通过实战来检验一下。

掌握了基础的事件流式与命名空间路由之后，大家可能也发现：stream_mode='updates'模式每次依然会等到信息积攒到一定量才输出，和常见的那种“逐字流式”体验还有差距。

本期笔者系统性地分享了 DeepAgents 流式输出的核心路径，并对stream_mode的几种模式进行了横向拆解与对比。

结合「一、流式输出为什么重要?」在本周做 1 次小范围对照验证

结合「二、架构基石：理解DeepAgents的流式机制」在本周做 1 次小范围对照验证

结合「三、DeepAgents流式输出实战」在本周做 1 次小范围对照验证

把本文一个关键认知转变写成 3 句话分享给同事

总结：

行动清单：

关键认知转变：从「记住结论」到「能判断何时该用、何时不该用」——边界感比信息量更重要。

讲解完毕。建议对照页面上的 SVG 卡片复习要点，并阅读原文加深理解。祝学习顺利！